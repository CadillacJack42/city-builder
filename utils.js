export const createCountString = (cityCount, waterCount, houseCount) => {
    return `You have changed Cities ${cityCount} times, visited the water ${waterCount} times, and moved ${houseCount} times`;
};